<?xml version="1.0" encoding="utf-8"?>
<manifest package="inleggo.com.myapplication"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISOS NECESARIOS INTERNET-->
     <uses-permission android:name="android.permission.INTERNET"/>
     <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

     <application
         android:allowBackup="true"
         android:icon="@mipmap/ic_launcher"
         android:label="@string/app_name"
         android:supportsRtl="true"
         android:theme="@style/AppTheme">
         <activity android:name=".ui.activity.LoginActivity">
             <intent-filter>
                 <action android:name="android.intent.action.MAIN"/>
                 <category android:name="android.intent.category.LAUNCHER"/>
             </intent-filter>
         </activity>
         <activity android:name=".ui.activity.CapturaActivity"
                     android:label="Captura"
                     android:theme="@style/AppTheme">
         </activity>
         <activity android:name=".ui.activity.MenuActivity"
             android:label="Menú Principal"
             android:theme="@style/AppTheme">
         </activity>
     </application>

 </manifest>